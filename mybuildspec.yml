version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
      - NOTHING TO BUILD AS IT IS TEST PIPELINE
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - "**/*"                   # to get all files of  GITHUB repo in s3 bicket
